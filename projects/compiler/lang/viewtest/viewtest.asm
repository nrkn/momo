; generated by momo - do not edit

        cpu     8086                        ; enforce the strict-8086 subset
        org     100h

; ---- constants: no storage, folded at assembly time ----
ioBase:         equ     10
ioZeroChar:     equ     48

; =========================================================== entry ====

__entry:
; ---- head[0] = 10
        mov     byte [head], 10
; ---- mid[0]  = 20
        mov     byte [mid], 20
; ---- tail[0] = 30
        mov     byte [tail], 30
; ---- putNumber( buf[0] )                        // 10
        mov     al, [buf]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( buf[4] )                        // 20
        mov     al, [buf + 4]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( buf[8] )                        // 30
        mov     al, [buf + 8]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- midPair[0] = 77
        mov     byte [midPair], 77
; ---- putNumber( buf[5] )                        // 77
        mov     al, [buf + 5]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( addr( head ) - addr( buf ) )    // 0
        mov     ax, head                    ; link-time constant
        push    ax                          ; save lhs: rhs is not a leaf
        mov     ax, buf                     ; link-time constant
        mov     bx, ax
        pop     ax
        sub     ax, bx
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( addr( mid ) - addr( buf ) )     // 4
        mov     ax, mid                     ; link-time constant
        push    ax                          ; save lhs: rhs is not a leaf
        mov     ax, buf                     ; link-time constant
        mov     bx, ax
        pop     ax
        sub     ax, bx
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( addr( midPair ) - addr( buf ) ) // 5
        mov     ax, midPair                 ; link-time constant
        push    ax                          ; save lhs: rhs is not a leaf
        mov     ax, buf                     ; link-time constant
        mov     bx, ax
        pop     ax
        sub     ax, bx
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( len( head ) )                   // 4
        mov     word [putNumber__n], 4
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( len( tail ) )                   // 4
        mov     word [putNumber__n], 4
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( len( words ) )                  // 6
        mov     word [putNumber__n], 6
        call    putNumber
; ---- newline()
        call    newline
; ---- buf[1] = 2
        mov     byte [buf + 1], 2
; ---- putNumber( words[0] )                      // 2 * 256 + 10 = 522
        mov     ax, [words]
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( frozen[0] )                     // 10
        mov     al, [frozen]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- total = 0
        mov     word [total], 0
; ---- for ( u16 i in tail ) {
        mov     word [i], 0
.L1:
        mov     ax, [i]
        cmp     ax, 4
        jae     .L3                         ; unsigned <
; ---- tail[i] = u8( i * 3 + 1 )
        mov     ax, [i]
        mov     bx, 3
        mul     bx                          ; low 16 bits are sign-agnostic
        inc     ax
        xor     ah, ah                      ; cast to u8
        push    ax                          ; save value while computing the index
        mov     ax, [i]
        mov     bx, ax
        pop     ax
        mov     [tail + bx], al
; ---- total += tail[i]
        mov     ax, [total]
        push    ax                          ; save lhs: rhs is not a leaf
        mov     ax, [i]
        mov     bx, ax
        mov     al, [tail + bx]
        xor     ah, ah                      ; u8 -> u16
        mov     bx, ax
        pop     ax
        add     ax, bx
        mov     [total], ax
.L2:
        inc     word [i]
        jmp     .L1
.L3:
; ---- putNumber( total )                         // 1 + 4 + 7 + 10 = 22
        mov     ax, [total]
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( buf[11] )                       // the last element of tail, so 10
        mov     al, [buf + 11]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- playerX  = 3
        mov     byte [playerX], 3
; ---- playerY  = 4
        mov     byte [playerY], 4
; ---- playerHp = 1000
        mov     word [playerHp], 1000
; ---- putNumber( player[0] )                     // 3
        mov     al, [player]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( player[1] )                     // 4
        mov     al, [player + 1]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( player[2] )                     // 0xE8 = 232
        mov     al, [player + 2]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( player[3] )                     // 0x03 = 3
        mov     al, [player + 3]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( playerHp )                      // 1000
        mov     ax, [playerHp]
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( tagPair[0] )                    // 'h' = 104
        mov     al, [tagPair]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- mapData[15] = 99
        mov     byte [mapData + 15], 99
; ---- entities[0] = 1234
        mov     word [entities], 1234
; ---- entities[7] = 4321
        mov     word [entities + 14], 4321
; ---- putNumber( mapData[15] )                   // 99, undisturbed by the words above
        mov     al, [mapData + 15]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( entities[0] )                   // 1234
        mov     ax, [entities]
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( entities[7] )                   // 4321
        mov     ax, [entities + 14]
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( _heapw[8] )                     // 1234
        mov     ax, [_heapw + 16]
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- setTextMode()
        call    setTextMode
; ---- row2[0] = 0x0741                           // 'A', attribute 7
        mov     dx, 0xB800                  ; segment of row2
        mov     es, dx
        mov     word [es:320], 1857
; ---- putNumber( cells[160] )                    // 0x0741 = 1857
        mov     dx, 0xB800                  ; segment of cells
        mov     es, dx
        mov     ax, [es:320]
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline

; ---- implicit exit ----
        mov     word [_ax], 0x4C00          ; DOS terminate, exit code 0
        call    int21

; ============================================== sub putChar ====

putChar:
; ---- _ah = 0x02
        mov     byte [_ah], 2
; ---- _dl = c
        mov     al, [putChar__c]
        mov     [_dl], al                   ; u8 -> u8, no widening
; ---- int 0x21
        call    int21
        ret

; ============================================== sub newline ====

newline:
; ---- putChar(13)
        mov     byte [putChar__c], 13
        call    putChar
; ---- putChar(10)
        mov     byte [putChar__c], 10
        call    putChar
        ret

; ============================================== sub putNumber ====

putNumber:
; ---- if (n == 0) {
        mov     ax, [putNumber__n]
        test    ax, ax
        jne     .L5                         ; unsigned ==
; ---- putChar(ioZeroChar)
        mov     byte [putChar__c], 48
        call    putChar
; ---- return
        ret
.L5:
; ---- for (i = 0; n > 0; i++) {
        mov     byte [putNumber__i], 0
.L8:
        mov     ax, [putNumber__n]
        test    ax, ax
        jbe     .L10                        ; unsigned >
; ---- digits[i] = u8(n % ioBase) + ioZeroChar
        mov     ax, [putNumber__n]
        mov     bx, 10
        xor     dx, dx                      ; clear high half for div
        div     bx
        mov     ax, dx                      ; remainder
        xor     ah, ah                      ; cast to u8
        add     ax, 48
        push    ax                          ; save value while computing the index
        mov     al, [putNumber__i]
        xor     ah, ah                      ; u8 -> u16
        mov     bx, ax
        pop     ax
        mov     [putNumber__digits + bx], al
; ---- n /= ioBase
        mov     ax, [putNumber__n]
        mov     bx, 10
        xor     dx, dx                      ; clear high half for div
        div     bx
        mov     [putNumber__n], ax
.L9:
        inc     byte [putNumber__i]
        jmp     .L8
.L10:
; ---- for (; i > 0; i--) {
.L12:
        mov     al, [putNumber__i]
        test    al, al
        jbe     .L14                        ; unsigned >
; ---- putChar(digits[i - 1])
        mov     al, [putNumber__i]
        xor     ah, ah                      ; u8 -> u16
        dec     ax
        mov     bx, ax
        mov     al, [putNumber__digits + bx]
        mov     [putChar__c], al            ; u8 -> u8, no widening
        call    putChar
.L13:
        dec     byte [putNumber__i]
        jmp     .L12
.L14:
        ret

; ============================================== sub setTextMode ====

setTextMode:
; ---- _ax = 0x0003
        mov     word [_ax], 3
; ---- int 0x10
        call    int10
        ret

; ==================================================== int helpers ====
; One per distinct interrupt: the literal is baked in, so the register
; sync is emitted once rather than at every call site.

int10:
        push    es
        mov     ax, [_ax]
        mov     bx, [_bx]
        mov     cx, [_cx]
        mov     dx, [_dx]
        mov     si, [_si]
        mov     di, [_di]
        int     0x10
        mov     [_ax], ax
        mov     [_bx], bx
        mov     [_cx], cx
        mov     [_dx], dx
        mov     [_si], si
        mov     [_di], di
        pop     es
        ret

int21:
        push    es
        mov     ax, [_ax]
        mov     bx, [_bx]
        mov     cx, [_cx]
        mov     dx, [_dx]
        mov     si, [_si]
        mov     di, [_di]
        int     0x21
        mov     [_ax], ax
        mov     [_bx], bx
        mov     [_cx], cx
        mov     [_dx], dx
        mov     [_si], si
        mov     [_di], di
        pop     es
        ret

; ============================================================ data ====

; ---- reserved globals: the machine registers ----
_ax:            dw      0
_al:            equ     _ax
_ah:            equ     _ax + 1
_bx:            dw      0
_bl:            equ     _bx
_bh:            equ     _bx + 1
_cx:            dw      0
_cl:            equ     _cx
_ch:            equ     _cx + 1
_dx:            dw      0
_dl:            equ     _dx
_dh:            equ     _dx + 1
_si:            dw      0
_di:            dw      0

; ---- variables ----
i:              dw      0        ; u16
putChar__c:     db      0        ; u8
putNumber__n:   dw      0        ; u16
total:          dw      0        ; u16
putNumber__i:   db      0        ; u8

; ---- arrays ----
buf:            times 12 db 0        ; u8[12]
player:         times 4 db 0        ; u8[4]
tag:            db      'hi$'        ; u8[3] const
putNumber__digits: times 5 db 0        ; u8[5]

; ============================================================ heap ====
; No storage is emitted - a .COM owns everything past its image, so
; these are addresses and NASM does the arithmetic.

_hstack:        equ     264        ; 8 worst-case + 256 interrupt reserve
_htop:          equ     0FFFEh - _hstack

_hsize:         dw      _htop - _heap        ; NASM computes this
        align   2                           ; keep the u16 view aligned
_heap:
_heapw:         equ     _heap        ; same bytes, u16 view

; =========================================================== views ====
; No storage: each is a name for an offset into something else.

head:           equ     buf        ; u8[4]
mid:            equ     buf + 4        ; u8[4]
tail:           equ     buf + 8        ; u8[4]
midPair:        equ     buf + 5        ; u8[2]
words:          equ     buf        ; u16[6]
frozen:         equ     buf        ; u8[4] const
playerX:        equ     player        ; u8
playerY:        equ     player + 1        ; u8
playerHp:       equ     player + 2        ; u16
tagPair:        equ     tag        ; u8[2] const
mapData:        equ     _heap        ; u8[16]
entities:       equ     _heap + 16        ; u16[8]
