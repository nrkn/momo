; generated by momo - do not edit

        cpu     8086                        ; enforce the strict-8086 subset
        org     100h

; ---- constants: no storage, folded at assembly time ----
ioBase:         equ     10
ioZeroChar:     equ     48
k:              equ     65535
z:              equ     0
m:              equ     300
p:              equ     32767
b:              equ     5
d:              equ     1005
n:              equ     -7

; =========================================================== entry ====

__entry:
; ---- putNumber( k + 1 )            // 65536 wraps to 0
        mov     word [putNumber__n], 0
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( z - 1 )            // -1 wraps to 65535
        mov     word [putNumber__n], 65535
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( m * 300 )          // 90000 wraps to 24464
        mov     word [putNumber__n], 24464
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( u16( p + 1 ) )     // 32768 wraps to -32768, same bits
        mov     word [putNumber__n], 32768
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( b << 14 )          // 0x14000 keeps its low word: 16384
        mov     word [putNumber__n], 16384
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( vk + 1 )
        mov     ax, [vk]
        inc     ax
        mov     [putNumber__n], ax
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( vz - 1 )
        mov     ax, [vz]
        dec     ax
        mov     [putNumber__n], ax
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( vm * 300 )
        mov     ax, [vm]
        mov     bx, 300
        mul     bx                          ; low 16 bits are sign-agnostic
        mov     [putNumber__n], ax
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( u16( vp + 1 ) )
        mov     ax, [vp]
        inc     ax
        mov     [putNumber__n], ax
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( vb << 14 )
        mov     ax, [vb]
        mov     cl, 14                      ; 8086 has no shift-by-immediate
        shl     ax, cl
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( k + 1 == 0 ? 1 : 0 )       // wraps to 0, so true
        mov     word [putNumber__n], 1
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( m * 300 < 1000 ? 1 : 0 )   // wraps to 24464, so false
        mov     word [putNumber__n], 0
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( p + 1 < 0 ? 1 : 0 )        // wraps negative, so true
        mov     word [putNumber__n], 1
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( vk + 1 == 0 ? 1 : 0 )
        mov     ax, [vk]
        inc     ax
        test    ax, ax
        jne     .L1                         ; unsigned ==
        mov     ax, 1
        jmp     .L2
.L1:
        xor     ax, ax                      ; 0
.L2:
        mov     [putNumber__n], ax
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( vm * 300 < 1000 ? 1 : 0 )
        mov     ax, [vm]
        mov     bx, 300
        mul     bx                          ; low 16 bits are sign-agnostic
        cmp     ax, 1000
        jae     .L4                         ; unsigned <
        mov     ax, 1
        jmp     .L5
.L4:
        xor     ax, ax                      ; 0
.L5:
        mov     [putNumber__n], ax
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( vp + 1 < 0 ? 1 : 0 )
        mov     ax, [vp]
        inc     ax
        test    ax, ax
        jge     .L7                         ; signed <
        mov     ax, 1
        jmp     .L8
.L7:
        xor     ax, ax                      ; 0
.L8:
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( d / 8 )            // 125
        mov     word [putNumber__n], 125
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( d % 8 )            // 5
        mov     word [putNumber__n], 5
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( u16( n / 2 ) )     // -3, and NOT -4: trunc, not floor
        mov     word [putNumber__n], 65533
        call    putNumber
; ---- newline()
        call    newline
; ---- putNumber( vd / 8 )
        mov     ax, [vd]
        mov     cl, 3                       ; 8086 has no shift-by-immediate
        shr     ax, cl                      ; / 8 is >> 3
        mov     [putNumber__n], ax
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( vd % 8 )
        mov     ax, [vd]
        and     ax, 7                       ; % 8 is a mask
        mov     [putNumber__n], ax
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( u16( vn / 2 ) )
        mov     ax, [vn]
        mov     bx, 2
        cwd                                 ; sign-extend AX into DX:AX
        idiv    bx
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline

; ---- implicit exit ----
        mov     word [_ax], 0x4C00          ; DOS terminate, exit code 0
        call    int21

; ============================================== sub putChar ====

putChar:
; ---- _ah = 0x02
        mov     byte [_ah], 2
; ---- _dl = c
        mov     al, [putChar__c]
        mov     [_dl], al                   ; u8 -> u8, no widening
; ---- int 0x21
        call    int21
        ret

; ============================================== sub newline ====

newline:
; ---- putChar(13)
        mov     byte [putChar__c], 13
        call    putChar
; ---- putChar(10)
        mov     byte [putChar__c], 10
        call    putChar
        ret

; ============================================== sub space ====

space:
; ---- sub space => putChar(32)
        mov     byte [putChar__c], 32
        call    putChar
        ret

; ============================================== sub putNumber ====

putNumber:
; ---- if (n == 0) {
        mov     ax, [putNumber__n]
        test    ax, ax
        jne     .L10                        ; unsigned ==
; ---- putChar(ioZeroChar)
        mov     byte [putChar__c], 48
        call    putChar
; ---- return
        ret
.L10:
; ---- for (i = 0; n > 0; i++) {
        mov     byte [putNumber__i], 0
.L13:
        mov     ax, [putNumber__n]
        test    ax, ax
        jbe     .L15                        ; unsigned >
; ---- digits[i] = u8(n % ioBase) + ioZeroChar
        mov     ax, [putNumber__n]
        mov     bx, 10
        xor     dx, dx                      ; clear high half for div
        div     bx
        mov     ax, dx                      ; remainder
        xor     ah, ah                      ; cast to u8
        add     ax, 48
        mov     bl, [putNumber__i]
        xor     bh, bh                      ; u8 -> u16
        mov     [putNumber__digits + bx], al
; ---- n /= ioBase
        mov     ax, [putNumber__n]
        mov     bx, 10
        xor     dx, dx                      ; clear high half for div
        div     bx
        mov     [putNumber__n], ax
.L14:
        inc     byte [putNumber__i]
        jmp     .L13
.L15:
; ---- for (; i > 0; i--) {
.L17:
        mov     al, [putNumber__i]
        test    al, al
        jbe     .L19                        ; unsigned >
; ---- putChar(digits[i - 1])
        mov     al, [putNumber__i]
        xor     ah, ah                      ; u8 -> u16
        dec     ax
        mov     bx, ax
        mov     al, [putNumber__digits + bx]
        mov     [putChar__c], al            ; u8 -> u8, no widening
        call    putChar
.L18:
        dec     byte [putNumber__i]
        jmp     .L17
.L19:
        ret

; ==================================================== int helpers ====
; One per distinct interrupt: the literal is baked in, so the register
; sync is emitted once rather than at every call site.

int21:
        mov     ax, [_ax]
        mov     bx, [_bx]
        mov     cx, [_cx]
        mov     dx, [_dx]
        mov     si, [_si]
        mov     di, [_di]
        int     0x21
        mov     [_ax], ax
        mov     [_bx], bx
        mov     [_cx], cx
        mov     [_dx], dx
        mov     [_si], si
        mov     [_di], di
        ret

; ============================================================ data ====

; ---- reserved globals: the machine registers ----
_ax:            dw      0
_al:            equ     _ax
_ah:            equ     _ax + 1
_bx:            dw      0
_bl:            equ     _bx
_bh:            equ     _bx + 1
_cx:            dw      0
_cl:            equ     _cx
_ch:            equ     _cx + 1
_dx:            dw      0
_dl:            equ     _dx
_dh:            equ     _dx + 1
_si:            dw      0
_di:            dw      0

; ---- variables ----
putChar__c:     db      0        ; u8
putNumber__n:   dw      0        ; u16
vk:             dw      65535        ; u16 = 65535
vz:             dw      0        ; u16
vm:             dw      300        ; u16 = 300
vp:             dw      32767        ; i16 = 32767
vb:             dw      5        ; u16 = 5
vd:             dw      1005        ; u16 = 1005
vn:             dw      65529        ; i16 = -7
putNumber__i:   db      0        ; u8

; ---- arrays ----
putNumber__digits: times 5 db 0        ; u8[5]

; ============================================================ heap ====
; No storage is emitted - a .COM owns everything past its image, so
; these are addresses and NASM does the arithmetic.

_hstack:        equ     260        ; 4 worst-case + 256 interrupt reserve
_htop:          equ     0FFFEh - _hstack

_hsize:         dw      _htop - _heap        ; NASM computes this
        align   2                           ; keep the u16 view aligned
_heap:
_heapw:         equ     _heap        ; same bytes, u16 view
