; generated by momo - do not edit

        cpu     8086                        ; enforce the strict-8086 subset
        org     100h

; ---- constants: no storage, folded at assembly time ----
ioBase:         equ     10
ioZeroChar:     equ     48
peak:           equ     60
lastCol:        equ     79

; =========================================================== entry ====

__entry:
; ---- for ( v of ramp ) {
        mov     word [of__unitrng__0], 0
.L1:
        mov     ax, [of__unitrng__0]
        cmp     ax, 12
        jae     .L3                         ; unsigned <
; ---- sum += u16( v )
        mov     ax, [sum]
        push    ax                          ; save lhs: rhs is not a leaf
        mov     ax, [of__unitrng__0]
        mov     bx, ax
        mov     al, [ramp + bx]
        xor     ah, ah                      ; u8 -> u16
        mov     bx, ax
        pop     ax
        add     ax, bx
        mov     [sum], ax
.L2:
        inc     word [of__unitrng__0]
        jmp     .L1
.L3:
; ---- putNumber( sum )
        mov     ax, [sum]
        mov     [putNumber__n], ax
        call    putNumber
; ---- for ( u16 i = 0; i < 3; i++ ) {
        mov     word [i], 0
.L5:
        mov     ax, [i]
        cmp     ax, 3
        jae     .L7                         ; unsigned <
; ---- space()
        call    space
; ---- putNumber( u16( swatch[i].r ) + u16( swatch[i].g ) + u16( swatch[i].b ) )
        mov     ax, [i]
        mov     bx, ax
        mov     al, [swatch__r + bx]
        xor     ah, ah                      ; u8 -> u16
        push    ax                          ; save lhs: rhs is not a leaf
        mov     ax, [i]
        mov     bx, ax
        mov     al, [swatch__g + bx]
        xor     ah, ah                      ; u8 -> u16
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        mov     ax, [i]
        mov     bx, ax
        mov     al, [swatch__b + bx]
        xor     ah, ah                      ; u8 -> u16
        mov     bx, ax
        pop     ax
        add     ax, bx
        mov     [putNumber__n], ax
        call    putNumber
.L6:
        inc     word [i]
        jmp     .L5
.L7:
; ---- newline()
        call    newline
; ---- putNumber( u16( level ) )
        mov     al, [level]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( u16( col ) )
        mov     al, [col]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
; ---- space()
        call    space
; ---- putNumber( u16( spot ) )
        mov     ax, [spot]
        mov     [putNumber__n], ax
        call    putNumber
; ---- level = 40                      // an assignment
        mov     byte [level], 40
; ---- show( level )
        mov     al, [level]
        mov     [show__v], al               ; u8 -> u8, no widening
        call    show
; ---- show( 63 )                      // an argument, on the bound
        mov     byte [show__v], 63
        call    show
; ---- show( dim() )
        call    dim
        mov     al, [dim__ret]
        xor     ah, ah                      ; u8 -> u16
        mov     [show__v], al               ; narrowed to u8
        call    show
; ---- show( intensity( 62 ) )         // a cast of a constant
        mov     byte [show__v], 62
        call    show
; ---- col = column( 79 )
        mov     byte [col], 79
; ---- space()
        call    space
; ---- putNumber( u16( col ) )
        mov     al, [col]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- level = peak + 3                // a folded constant of the unit: 63, in range
        mov     byte [level], 63
; ---- putNumber( u16( level ) )
        mov     al, [level]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
; ---- col = lastCol - 9
        mov     byte [col], 70
; ---- space()
        call    space
; ---- putNumber( u16( col ) )
        mov     al, [col]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
; ---- level = intensity( byte )       // runtime: the cast emits nothing, 200 goes in
        mov     al, [byte_]
        xor     ah, ah                      ; u8 -> u16
        mov     [level], al                 ; narrowed to u8
; ---- show( level )
        mov     [show__v], al               ; u8 -> u8, no widening
        call    show
; ---- level = level + 10              // arithmetic re-proves nothing
        mov     al, [level]
        xor     ah, ah                      ; u8 -> u16
        add     ax, 10
        mov     [level], al                 ; narrowed to u8
; ---- show( level )
        mov     [show__v], al               ; u8 -> u8, no widening
        call    show
; ---- newline()
        call    newline

; ---- implicit exit ----
        mov     word [_ax], 0x4C00          ; DOS terminate, exit code 0
        call    int21

; ============================================== sub putChar ====

putChar:
; ---- _ah = 0x02
        mov     byte [_ah], 2
; ---- _dl = c
        mov     al, [putChar__c]
        mov     [_dl], al                   ; u8 -> u8, no widening
; ---- int 0x21
        call    int21
        ret

; ============================================== sub newline ====

newline:
; ---- putChar(13)
        mov     byte [putChar__c], 13
        call    putChar
; ---- putChar(10)
        mov     byte [putChar__c], 10
        call    putChar
        ret

; ============================================== sub space ====

space:
; ---- sub space => putChar(32)
        mov     byte [putChar__c], 32
        call    putChar
        ret

; ============================================== sub putNumber ====

putNumber:
; ---- if (n == 0) {
        mov     ax, [putNumber__n]
        test    ax, ax
        jne     .L9                         ; unsigned ==
; ---- putChar(ioZeroChar)
        mov     byte [putChar__c], 48
        call    putChar
; ---- return
        ret
.L9:
; ---- for (i = 0; n > 0; i++) {
        mov     byte [putNumber__i], 0
.L12:
        mov     ax, [putNumber__n]
        test    ax, ax
        jbe     .L14                        ; unsigned >
; ---- digits[i] = u8(n % ioBase) + ioZeroChar
        mov     ax, [putNumber__n]
        mov     bx, 10
        xor     dx, dx                      ; clear high half for div
        div     bx
        mov     ax, dx                      ; remainder
        xor     ah, ah                      ; cast to u8
        add     ax, 48
        mov     bl, [putNumber__i]
        xor     bh, bh                      ; u8 -> u16
        mov     [putNumber__digits + bx], al
; ---- n /= ioBase
        mov     ax, [putNumber__n]
        mov     bx, 10
        xor     dx, dx                      ; clear high half for div
        div     bx
        mov     [putNumber__n], ax
.L13:
        inc     byte [putNumber__i]
        jmp     .L12
.L14:
; ---- for (; i > 0; i--) {
.L16:
        mov     al, [putNumber__i]
        test    al, al
        jbe     .L18                        ; unsigned >
; ---- putChar(digits[i - 1])
        mov     al, [putNumber__i]
        xor     ah, ah                      ; u8 -> u16
        dec     ax
        mov     bx, ax
        mov     al, [putNumber__digits + bx]
        mov     [putChar__c], al            ; u8 -> u8, no widening
        call    putChar
.L17:
        dec     byte [putNumber__i]
        jmp     .L16
.L18:
        ret

; ============================================== sub show ====

show:
; ---- space()
        call    space
; ---- putNumber( u16( v ) )
        mov     al, [show__v]
        xor     ah, ah                      ; u8 -> u16
        mov     [putNumber__n], ax
        call    putNumber
        ret

; ============================================== u8 dim ====

dim:
; ---- intensity dim() => 21           // a return
        mov     byte [dim__ret], 21
        ret

; ==================================================== int helpers ====
; One per distinct interrupt: the literal is baked in, so the register
; sync is emitted once rather than at every call site.

int21:
        mov     ax, [_ax]
        mov     bx, [_bx]
        mov     cx, [_cx]
        mov     dx, [_dx]
        mov     si, [_si]
        mov     di, [_di]
        int     0x21
        mov     [_ax], ax
        mov     [_bx], bx
        mov     [_cx], cx
        mov     [_dx], dx
        mov     [_si], si
        mov     [_di], di
        ret

; ============================================================ data ====

; ---- reserved globals: the machine registers ----
_ax:            dw      0
_al:            equ     _ax
_ah:            equ     _ax + 1
_bx:            dw      0
_bl:            equ     _bx
_bh:            equ     _bx + 1
_cx:            dw      0
_cl:            equ     _cx
_ch:            equ     _cx + 1
_dx:            dw      0
_dl:            equ     _dx
_dh:            equ     _dx + 1
_si:            dw      0
_di:            dw      0

; ---- variables ----
of__unitrng__0: dw      0        ; u16
i:              dw      0        ; u16
putChar__c:     db      0        ; u8
putNumber__n:   dw      0        ; u16
level:          db      63        ; u8 = 63
col:            db      79        ; u8 = 79
spot:           dw      1999        ; u16 = 1999
byte_:          db      200        ; u8 = 200
sum:            dw      0        ; u16
show__v:        db      0        ; u8
dim__ret:       db      0        ; u8
putNumber__i:   db      0        ; u8

; ---- arrays ----
ramp:           db      0, 7, 14, 21, 28, 35, 42, 49, 56, 60, 62, 63        ; u8[12] const
swatch__r:      db      63, 0, 42        ; u8[3]
swatch__g:      db      0, 63, 42        ; u8[3]
swatch__b:      db      0, 21, 63        ; u8[3]
putNumber__digits: times 5 db 0        ; u8[5]

; ============================================================ heap ====
; No storage is emitted - a .COM owns everything past its image, so
; these are addresses and NASM does the arithmetic.

_hstack:        equ     264        ; 8 worst-case + 256 interrupt reserve
_htop:          equ     0FFFEh - _hstack

_hsize:         dw      _htop - _heap        ; NASM computes this
        align   2                           ; keep the u16 view aligned
_heap:
_heapw:         equ     _heap        ; same bytes, u16 view
