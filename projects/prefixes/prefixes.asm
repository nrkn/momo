; generated by momo - do not edit

        cpu     8086                        ; enforce the strict-8086 subset
        org     100h

; ---- constants: no storage, folded at assembly time ----
ioBase:         equ     10
ioZeroChar:     equ     48
push:           equ     256

; =========================================================== entry ====

__entry:
; ---- total = wait() + lock() + rep() + repe() + repz() + repne() + repnz() + a16()
        call    wait_
        mov     ax, [wait___ret]
        push    ax                          ; save lhs: rhs is not a leaf
        call    lock_
        mov     ax, [lock___ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        call    rep_
        mov     ax, [rep___ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        call    repe_
        mov     ax, [repe___ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        call    repz_
        mov     ax, [repz___ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        call    repne_
        mov     ax, [repne___ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        call    repnz_
        mov     ax, [repnz___ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        call    a16_
        mov     ax, [a16___ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        mov     [total], ax
; ---- total = total + a32() + o16() + o32() + xacquire() + xrelease() + bnd() + nobnd()
        mov     ax, [total]
        push    ax                          ; save lhs: rhs is not a leaf
        call    a32_
        mov     ax, [a32___ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        call    o16_
        mov     ax, [o16___ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        call    o32_
        mov     ax, [o32___ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        call    xacquire_
        mov     ax, [xacquire___ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        call    xrelease_
        mov     ax, [xrelease___ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        call    bnd_
        mov     ax, [bnd___ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        call    nobnd_
        mov     ax, [nobnd___ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        mov     [total], ax
; ---- putNumber( total )
        mov     ax, [total]
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- total = absolute() + times() + es() + word() + add()
        call    absolute_
        mov     ax, [absolute___ret]
        push    ax                          ; save lhs: rhs is not a leaf
        call    times_
        mov     ax, [times___ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        call    es_
        mov     ax, [es___ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        call    word_
        mov     ax, [word___ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        call    add
        mov     ax, [add__ret]
        mov     bx, ax
        pop     ax
        add     ax, bx
        mov     [total], ax
; ---- putNumber( total )
        mov     ax, [total]
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline
; ---- mov = 8
        mov     word [mov], 8
; ---- xor = 16
        mov     byte [xor], 16
; ---- nop = 2048
        mov     word [nop], 2048
; ---- cmp[0] = 32
        mov     word [cmp], 32
; ---- cmp[1] = 64
        mov     word [cmp + 2], 64
; ---- cmp[2] = 128
        mov     word [cmp + 4], 128
; ---- ret[0].dec = 512
        mov     word [ret__dec], 512
; ---- ret[1].dec = 1024
        mov     word [ret__dec + 2], 1024
; ---- total = jmp[0] + jmp[1] + jmp[2] + mov + xor
        mov     al, [jmp]
        xor     ah, ah                      ; u8 -> u16
        push    ax                          ; save lhs: rhs is not a leaf
        mov     al, [jmp + 1]
        xor     ah, ah                      ; u8 -> u16
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        mov     al, [jmp + 2]
        xor     ah, ah                      ; u8 -> u16
        mov     bx, ax
        pop     ax
        add     ax, bx
        mov     bx, [mov]
        add     ax, bx
        mov     bl, [xor]
        xor     bh, bh                      ; u8 -> u16
        add     ax, bx
        mov     [total], ax
; ---- total = total + pop[0] + pop[1] + cmp[2] + push
        mov     ax, [total]
        push    ax                          ; save lhs: rhs is not a leaf
        mov     ax, [pop]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        mov     ax, [pop + 2]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        mov     ax, [cmp + 4]
        mov     bx, ax
        pop     ax
        add     ax, bx
        add     ax, 256
        mov     [total], ax
; ---- total = total + ret[0].dec + ret[1].dec + nop
        mov     ax, [total]
        push    ax                          ; save lhs: rhs is not a leaf
        mov     ax, [ret__dec]
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        mov     ax, [ret__dec + 2]
        mov     bx, ax
        pop     ax
        add     ax, bx
        mov     bx, [nop]
        add     ax, bx
        mov     [total], ax
; ---- putNumber( total )
        mov     ax, [total]
        mov     [putNumber__n], ax
        call    putNumber
; ---- newline()
        call    newline

; ---- implicit exit ----
        mov     word [_ax], 0x4C00          ; DOS terminate, exit code 0
        call    int21

; ============================================== sub putChar ====

putChar:
; ---- _ah = 0x02
        mov     byte [_ah], 2
; ---- _dl = c
        mov     al, [putChar__c]
        mov     [_dl], al                   ; u8 -> u8, no widening
; ---- int 0x21
        call    int21
        ret

; ============================================== sub newline ====

newline:
; ---- putChar(13)
        mov     byte [putChar__c], 13
        call    putChar
; ---- putChar(10)
        mov     byte [putChar__c], 10
        call    putChar
        ret

; ============================================== sub putNumber ====

putNumber:
; ---- if (n == 0) {
        mov     ax, [putNumber__n]
        test    ax, ax
        je      .L3                         ; unsigned ==
        jmp     .L1
.L3:
; ---- putChar(ioZeroChar)
        mov     byte [putChar__c], 48
        call    putChar
; ---- return
        ret
.L1:
; ---- for (i = 0; n > 0; i++) {
        mov     byte [putNumber__i], 0
.L4:
        mov     ax, [putNumber__n]
        test    ax, ax
        ja      .L7                         ; unsigned >
        jmp     .L6
.L7:
; ---- digits[i] = u8(n % ioBase) + ioZeroChar
        mov     ax, [putNumber__n]
        mov     bx, 10
        xor     dx, dx                      ; clear high half for div
        div     bx
        mov     ax, dx                      ; remainder
        xor     ah, ah                      ; cast to u8
        add     ax, 48
        push    ax                          ; save value while computing the index
        mov     al, [putNumber__i]
        xor     ah, ah                      ; u8 -> u16
        mov     bx, ax
        pop     ax
        mov     [putNumber__digits + bx], al
; ---- n /= ioBase
        mov     ax, [putNumber__n]
        mov     bx, 10
        xor     dx, dx                      ; clear high half for div
        div     bx
        mov     [putNumber__n], ax
.L5:
        inc     byte [putNumber__i]
        jmp     .L4
.L6:
; ---- for (; i > 0; i--) {
.L8:
        mov     al, [putNumber__i]
        test    al, al
        ja      .L11                        ; unsigned >
        jmp     .L10
.L11:
; ---- putChar(digits[i - 1])
        mov     al, [putNumber__i]
        xor     ah, ah                      ; u8 -> u16
        dec     ax
        mov     bx, ax
        mov     al, [putNumber__digits + bx]
        mov     [putChar__c], al            ; u8 -> u8, no widening
        call    putChar
.L9:
        dec     byte [putNumber__i]
        jmp     .L8
.L10:
        ret

; ============================================== u16 wait ====

wait_:
; ---- u16 wait() => 1
        mov     word [wait___ret], 1
        ret

; ============================================== u16 lock ====

lock_:
; ---- u16 lock() => 2
        mov     word [lock___ret], 2
        ret

; ============================================== u16 rep ====

rep_:
; ---- u16 rep() => 4
        mov     word [rep___ret], 4
        ret

; ============================================== u16 repe ====

repe_:
; ---- u16 repe() => 8
        mov     word [repe___ret], 8
        ret

; ============================================== u16 repz ====

repz_:
; ---- u16 repz() => 16
        mov     word [repz___ret], 16
        ret

; ============================================== u16 repne ====

repne_:
; ---- u16 repne() => 32
        mov     word [repne___ret], 32
        ret

; ============================================== u16 repnz ====

repnz_:
; ---- u16 repnz() => 64
        mov     word [repnz___ret], 64
        ret

; ============================================== u16 a16 ====

a16_:
; ---- u16 a16() => 128
        mov     word [a16___ret], 128
        ret

; ============================================== u16 a32 ====

a32_:
; ---- u16 a32() => 256
        mov     word [a32___ret], 256
        ret

; ============================================== u16 o16 ====

o16_:
; ---- u16 o16() => 512
        mov     word [o16___ret], 512
        ret

; ============================================== u16 o32 ====

o32_:
; ---- u16 o32() => 1024
        mov     word [o32___ret], 1024
        ret

; ============================================== u16 xacquire ====

xacquire_:
; ---- u16 xacquire() => 2048
        mov     word [xacquire___ret], 2048
        ret

; ============================================== u16 xrelease ====

xrelease_:
; ---- u16 xrelease() => 4096
        mov     word [xrelease___ret], 4096
        ret

; ============================================== u16 bnd ====

bnd_:
; ---- u16 bnd() => 8192
        mov     word [bnd___ret], 8192
        ret

; ============================================== u16 nobnd ====

nobnd_:
; ---- u16 nobnd() => 16384
        mov     word [nobnd___ret], 16384
        ret

; ============================================== u16 absolute ====

absolute_:
; ---- u16 absolute() => 1
        mov     word [absolute___ret], 1
        ret

; ============================================== u16 times ====

times_:
; ---- u16 times() => 2
        mov     word [times___ret], 2
        ret

; ============================================== u16 es ====

es_:
; ---- u16 es() => 4
        mov     word [es___ret], 4
        ret

; ============================================== u16 word ====

word_:
; ---- u16 word() => 8
        mov     word [word___ret], 8
        ret

; ============================================== u16 add ====

add:
; ---- u16 add() => 16
        mov     word [add__ret], 16
        ret

; ==================================================== int helpers ====
; One per distinct interrupt: the literal is baked in, so the register
; sync is emitted once rather than at every call site.

int21:
        mov     ax, [_ax]
        mov     bx, [_bx]
        mov     cx, [_cx]
        mov     dx, [_dx]
        mov     si, [_si]
        mov     di, [_di]
        int     0x21
        mov     [_ax], ax
        mov     [_bx], bx
        mov     [_cx], cx
        mov     [_dx], dx
        mov     [_si], si
        mov     [_di], di
        ret

; ============================================================ data ====

; ---- reserved globals: the machine registers ----
_ax:            dw      0
_al:            equ     _ax
_ah:            equ     _ax + 1
_bx:            dw      0
_bl:            equ     _bx
_bh:            equ     _bx + 1
_cx:            dw      0
_cl:            equ     _cx
_ch:            equ     _cx + 1
_dx:            dw      0
_dl:            equ     _dx
_dh:            equ     _dx + 1
_si:            dw      0
_di:            dw      0

; ---- variables ----
putChar__c:     db      0        ; u8
putNumber__n:   dw      0        ; u16
wait___ret:     dw      0        ; u16
lock___ret:     dw      0        ; u16
rep___ret:      dw      0        ; u16
repe___ret:     dw      0        ; u16
repz___ret:     dw      0        ; u16
repne___ret:    dw      0        ; u16
repnz___ret:    dw      0        ; u16
a16___ret:      dw      0        ; u16
a32___ret:      dw      0        ; u16
o16___ret:      dw      0        ; u16
o32___ret:      dw      0        ; u16
xacquire___ret: dw      0        ; u16
xrelease___ret: dw      0        ; u16
bnd___ret:      dw      0        ; u16
nobnd___ret:    dw      0        ; u16
absolute___ret: dw      0        ; u16
times___ret:    dw      0        ; u16
es___ret:       dw      0        ; u16
word___ret:     dw      0        ; u16
add__ret:       dw      0        ; u16
total:          dw      0        ; u16
mov:            dw      0        ; u16
xor:            db      0        ; u8
nop:            dw      0        ; u16
putNumber__i:   db      0        ; u8

; ---- arrays ----
cmp:            times 3 dw 0        ; u16[3]
jmp:            db      1, 2, 4        ; u8[3] const
ret__dec:       times 2 dw 0        ; u16[2]
putNumber__digits: times 5 db 0        ; u8[5]

; ============================================================ heap ====
; No storage is emitted - a .COM owns everything past its image, so
; these are addresses and NASM does the arithmetic.

_hstack:        equ     264        ; 8 worst-case + 256 interrupt reserve
_htop:          equ     0FFFEh - _hstack

_hsize:         dw      _htop - _heap        ; NASM computes this
        align   2                           ; keep the u16 view aligned
_heap:
_heapw:         equ     _heap        ; same bytes, u16 view

; =========================================================== views ====
; No storage: each is a name for an offset into something else.

pop:            equ     cmp        ; u16[2]
