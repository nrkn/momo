; generated by momo - do not edit

        cpu     8086                        ; enforce the strict-8086 subset
        org     100h

; ---- constants: no storage, folded at assembly time ----
videoSeg:       equ     47104

; =========================================================== entry ====

__entry:
; ---- cells[0] = 0x0741
        mov     dx, 0xB800                  ; segment of cells
        mov     es, dx
        mov     word [es:0], 1857
; ---- cells[i] = 0x0742
        mov     ax, [i]
        shl     ax, 1                       ; word elements
        mov     bx, ax
        mov     dx, 0xB800                  ; segment of cells
        mov     es, dx
        mov     word [es:bx], 1858
; ---- cells[i]++
        mov     ax, [i]
        shl     ax, 1                       ; word elements
        mov     bx, ax
        mov     dx, 0xB800                  ; segment of cells
        mov     es, dx
        inc     word [es:bx]
; ---- v = cells[0]
        mov     dx, 0xB800                  ; segment of cells
        mov     es, dx
        mov     ax, [es:0]
        mov     [v], ax
; ---- v = cells[i]
        mov     ax, [i]
        shl     ax, 1                       ; word elements
        mov     bx, ax
        mov     dx, 0xB800                  ; segment of cells
        mov     es, dx
        mov     ax, [es:bx]
        mov     [v], ax
; ---- b = cellBytes[3]
        mov     dx, 0xB800                  ; segment of cellBytes
        mov     es, dx
        mov     al, [es:3]
        mov     [b], al                     ; u8 -> u8, no widening
; ---- v = cellFive[0]
        mov     dx, 0xB800                  ; segment of cellFive
        mov     es, dx
        mov     ax, [es:10]
        mov     [v], ax
; ---- v = byConst[i]
        mov     ax, [i]
        shl     ax, 1                       ; word elements
        mov     bx, ax
        mov     dx, 0xB800                  ; segment of byConst
        mov     es, dx
        mov     ax, [es:bx]
        mov     [v], ax
; ---- b = unsized[i]
        mov     ax, [i]
        mov     bx, ax
        mov     dx, 0xA000                  ; segment of unsized
        mov     es, dx
        mov     al, [es:bx]
        mov     [b], al                     ; u8 -> u8, no widening
; ---- b = font[0]
        mov     dx, 0xF000                  ; segment of font
        mov     es, dx
        mov     al, [es:64110]
        mov     [b], al                     ; u8 -> u8, no widening
; ---- backBuffer[i] = 1
        mov     ax, [i]
        mov     bx, ax
        mov     dx, [seg_]                  ; segment of backBuffer
        mov     es, dx
        mov     byte [es:bx], 1
; ---- v = psp[1]
        mov     dx, ds                      ; segment of psp
        mov     es, dx
        mov     ax, [es:2]
        mov     [v], ax
; ---- v = len( cells )                            // a declared size is reportable
        mov     word [v], 2000

; ---- implicit exit ----
        mov     word [_ax], 0x4C00          ; DOS terminate, exit code 0
        call    int21

; ==================================================== int helpers ====
; One per distinct interrupt: the literal is baked in, so the register
; sync is emitted once rather than at every call site.

int21:
        push    es
        mov     ax, [_ax]
        mov     bx, [_bx]
        mov     cx, [_cx]
        mov     dx, [_dx]
        mov     si, [_si]
        mov     di, [_di]
        int     0x21
        mov     [_ax], ax
        mov     [_bx], bx
        mov     [_cx], cx
        mov     [_dx], dx
        mov     [_si], si
        mov     [_di], di
        pop     es
        ret

; ============================================================ data ====

; ---- reserved globals: the machine registers ----
_ax:            dw      0
_al:            equ     _ax
_ah:            equ     _ax + 1
_bx:            dw      0
_bl:            equ     _bx
_bh:            equ     _bx + 1
_cx:            dw      0
_cl:            equ     _cx
_ch:            equ     _cx + 1
_dx:            dw      0
_dl:            equ     _dx
_dh:            equ     _dx + 1
_si:            dw      0
_di:            dw      0

; ---- variables ----
seg_:           dw      0        ; u16
i:              dw      0        ; u16
v:              dw      0        ; u16
b:              db      0        ; u8

; ============================================================ heap ====
; No storage is emitted - a .COM owns everything past its image, so
; these are addresses and NASM does the arithmetic.

_hstack:        equ     256        ; 0 worst-case + 256 interrupt reserve
_htop:          equ     0FFFEh - _hstack

_hsize:         dw      _htop - _heap        ; NASM computes this
        align   2                           ; keep the u16 view aligned
_heap:
_heapw:         equ     _heap        ; same bytes, u16 view
