; generated by momo - do not edit

        cpu     8086                        ; enforce the strict-8086 subset
        org     100h

; =========================================================== entry ====

__entry:
; ---- twice()
        call    twice
; ---- nested()
        call    nested
; ---- blocks()
        call    blocks
; ---- arrowed()
        call    arrowed
; ---- for ( u8 t = 0; t < 2; t++ ) {
        mov     byte [t], 0
.L1:
        mov     al, [t]
        cmp     al, 2                       ; byte operands, no widening
        jae     .L3                         ; unsigned <
; ---- total = total + t
        mov     al, [total]
        xor     ah, ah                      ; u8 -> u16
        mov     bl, [t]
        xor     bh, bh                      ; u8 -> u16
        add     ax, bx
        mov     [total], al                 ; narrowed to u8
.L2:
        inc     byte [t]
        jmp     .L1
.L3:

; ---- implicit exit ----
        mov     word [_ax], 0x4C00          ; DOS terminate, exit code 0
        call    int21

; ============================================== sub twice ====

twice:
; ---- for ( u8 i = 0; i < 3; i++ ) {
        mov     byte [twice__i], 0
.L5:
        mov     al, [twice__i]
        cmp     al, 3                       ; byte operands, no widening
        jae     .L7                         ; unsigned <
; ---- total = total + i
        mov     al, [total]
        xor     ah, ah                      ; u8 -> u16
        mov     bl, [twice__i]
        xor     bh, bh                      ; u8 -> u16
        add     ax, bx
        mov     [total], al                 ; narrowed to u8
.L6:
        inc     byte [twice__i]
        jmp     .L5
.L7:
; ---- for ( u8 i = 0; i < 4; i++ ) {
        mov     byte [twice__i], 0
.L9:
        mov     al, [twice__i]
        cmp     al, 4                       ; byte operands, no widening
        jae     .L11                        ; unsigned <
; ---- total = total + i
        mov     al, [total]
        xor     ah, ah                      ; u8 -> u16
        mov     bl, [twice__i]
        xor     bh, bh                      ; u8 -> u16
        add     ax, bx
        mov     [total], al                 ; narrowed to u8
.L10:
        inc     byte [twice__i]
        jmp     .L9
.L11:
        ret

; ============================================== sub nested ====

nested:
; ---- for ( u16 a = 0; a < 2; a++ ) {
        mov     word [nested__a], 0
.L13:
        mov     ax, [nested__a]
        cmp     ax, 2
        jae     .L15                        ; unsigned <
; ---- for ( u8 b = 0; b < 2; b++ ) {
        mov     byte [nested__b], 0
.L17:
        mov     al, [nested__b]
        cmp     al, 2                       ; byte operands, no widening
        jae     .L19                        ; unsigned <
; ---- total = total + b
        mov     al, [total]
        xor     ah, ah                      ; u8 -> u16
        mov     bl, [nested__b]
        xor     bh, bh                      ; u8 -> u16
        add     ax, bx
        mov     [total], al                 ; narrowed to u8
.L18:
        inc     byte [nested__b]
        jmp     .L17
.L19:
.L14:
        inc     word [nested__a]
        jmp     .L13
.L15:
        ret

; ============================================== sub blocks ====

blocks:
; ---- if ( total > 0 ) {
        mov     al, [total]
        test    al, al
        jbe     .L21                        ; unsigned >
; ---- for ( u8 k = 0; k < 2; k++ ) {
        mov     byte [blocks__k], 0
.L24:
        mov     al, [blocks__k]
        cmp     al, 2                       ; byte operands, no widening
        jae     .L26                        ; unsigned <
; ---- total = total + k
        mov     al, [total]
        xor     ah, ah                      ; u8 -> u16
        mov     bl, [blocks__k]
        xor     bh, bh                      ; u8 -> u16
        add     ax, bx
        mov     [total], al                 ; narrowed to u8
.L25:
        inc     byte [blocks__k]
        jmp     .L24
.L26:
.L21:
; ---- for ( u8 k = 0; k < 3; k++ ) {
        mov     byte [blocks__k], 0
.L28:
        mov     al, [blocks__k]
        cmp     al, 3                       ; byte operands, no widening
        jae     .L30                        ; unsigned <
; ---- total = total + k
        mov     al, [total]
        xor     ah, ah                      ; u8 -> u16
        mov     bl, [blocks__k]
        xor     bh, bh                      ; u8 -> u16
        add     ax, bx
        mov     [total], al                 ; narrowed to u8
.L29:
        inc     byte [blocks__k]
        jmp     .L28
.L30:
        ret

; ============================================== sub arrowed ====

arrowed:
; ---- sub arrowed => for ( u8 z = 0; z < 2; z++ ) { total = total + z }
        mov     byte [arrowed__z], 0
.L32:
        mov     al, [arrowed__z]
        cmp     al, 2                       ; byte operands, no widening
        jae     .L34                        ; unsigned <
        mov     al, [total]
        xor     ah, ah                      ; u8 -> u16
        mov     bl, [arrowed__z]
        xor     bh, bh                      ; u8 -> u16
        add     ax, bx
        mov     [total], al                 ; narrowed to u8
.L33:
        inc     byte [arrowed__z]
        jmp     .L32
.L34:
        ret

; ==================================================== int helpers ====
; One per distinct interrupt: the literal is baked in, so the register
; sync is emitted once rather than at every call site.

int21:
        mov     ax, [_ax]
        mov     bx, [_bx]
        mov     cx, [_cx]
        mov     dx, [_dx]
        mov     si, [_si]
        mov     di, [_di]
        int     0x21
        mov     [_ax], ax
        mov     [_bx], bx
        mov     [_cx], cx
        mov     [_dx], dx
        mov     [_si], si
        mov     [_di], di
        ret

; ============================================================ data ====

; ---- reserved globals: the machine registers ----
_ax:            dw      0
_al:            equ     _ax
_ah:            equ     _ax + 1
_bx:            dw      0
_bl:            equ     _bx
_bh:            equ     _bx + 1
_cx:            dw      0
_cl:            equ     _cx
_ch:            equ     _cx + 1
_dx:            dw      0
_dl:            equ     _dx
_dh:            equ     _dx + 1
_si:            dw      0
_di:            dw      0

; ---- variables ----
t:              db      0        ; u8
total:          db      0        ; u8
twice__i:       db      0        ; u8
nested__a:      dw      0        ; u16
nested__b:      db      0        ; u8
blocks__k:      db      0        ; u8
arrowed__z:     db      0        ; u8

; ============================================================ heap ====
; No storage is emitted - a .COM owns everything past its image, so
; these are addresses and NASM does the arithmetic.

_hstack:        equ     258        ; 2 worst-case + 256 interrupt reserve
_htop:          equ     0FFFEh - _hstack

_hsize:         dw      _htop - _heap        ; NASM computes this
        align   2                           ; keep the u16 view aligned
_heap:
_heapw:         equ     _heap        ; same bytes, u16 view
