; generated by momo - do not edit

        cpu     8086                        ; enforce the strict-8086 subset
        org     100h

; =========================================================== entry ====

__entry:
; ---- out = apostrophe + backslash + quote + newline + carriage + tab + nul + mixed[0]
        mov     al, [apostrophe]
        xor     ah, ah                      ; u8 -> u16
        mov     bl, [backslash]
        xor     bh, bh                      ; u8 -> u16
        add     ax, bx
        mov     bl, [quote]
        xor     bh, bh                      ; u8 -> u16
        add     ax, bx
        mov     bl, [newline]
        xor     bh, bh                      ; u8 -> u16
        add     ax, bx
        mov     bl, [carriage]
        xor     bh, bh                      ; u8 -> u16
        add     ax, bx
        mov     bl, [tab]
        xor     bh, bh                      ; u8 -> u16
        add     ax, bx
        mov     bl, [nul]
        xor     bh, bh                      ; u8 -> u16
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        mov     al, [mixed]
        xor     ah, ah                      ; u8 -> u16
        mov     bx, ax
        pop     ax
        add     ax, bx
        mov     [out], al                   ; narrowed to u8

; ---- implicit exit ----
        mov     word [_ax], 0x4C00          ; DOS terminate, exit code 0
        call    int21

; ==================================================== int helpers ====
; One per distinct interrupt: the literal is baked in, so the register
; sync is emitted once rather than at every call site.

int21:
        mov     ax, [_ax]
        mov     bx, [_bx]
        mov     cx, [_cx]
        mov     dx, [_dx]
        mov     si, [_si]
        mov     di, [_di]
        int     0x21
        mov     [_ax], ax
        mov     [_bx], bx
        mov     [_cx], cx
        mov     [_dx], dx
        mov     [_si], si
        mov     [_di], di
        ret

; ============================================================ data ====

; ---- reserved globals: the machine registers ----
_ax:            dw      0
_al:            equ     _ax
_ah:            equ     _ax + 1
_bx:            dw      0
_bl:            equ     _bx
_bh:            equ     _bx + 1
_cx:            dw      0
_cl:            equ     _cx
_ch:            equ     _cx + 1
_dx:            dw      0
_dl:            equ     _dx
_dh:            equ     _dx + 1
_si:            dw      0
_di:            dw      0

; ---- variables ----
apostrophe:     db      39        ; u8 = 39
backslash:      db      92        ; u8 = 92
quote:          db      34        ; u8 = 34
newline:        db      10        ; u8 = 10
carriage:       db      13        ; u8 = 13
tab:            db      9        ; u8 = 9
nul:            db      0        ; u8
out:            db      0        ; u8

; ---- arrays ----
mixed:          db      'it', 39, 's "quoted"', 13, 10, '$'        ; u8[16] const

; ============================================================ heap ====
; No storage is emitted - a .COM owns everything past its image, so
; these are addresses and NASM does the arithmetic.

_hstack:        equ     258        ; 2 worst-case + 256 interrupt reserve
_htop:          equ     0FFFEh - _hstack

_hsize:         dw      _htop - _heap        ; NASM computes this
        align   2                           ; keep the u16 view aligned
_heap:
_heapw:         equ     _heap        ; same bytes, u16 view
