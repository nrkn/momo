// EXPECT-ERROR: 1 is a count, not a value with a radix point
// i12's whole part is not a type name, so this is also the spelling that catches
// a lexer building a fixed type by absorbing a dot after one.
i12.4 scale = 1
