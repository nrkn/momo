; generated by momo - do not edit

        cpu     8086                        ; enforce the strict-8086 subset
        org     100h

; ---- constants: no storage, folded at assembly time ----
mobCount:       equ     3

; =========================================================== entry ====

__entry:
; ---- for ( i = 0; i < len( buf ); i++ ) {
        mov     word [i], 0
.L1:
        mov     ax, [i]
        cmp     ax, 4
        jae     .L3                         ; unsigned <
; ---- buf[ i ] = u8( i )
        mov     ax, [i]
        mov     bx, [i]
        mov     [buf + bx], al
.L2:
        inc     word [i]
        jmp     .L1
.L3:
; ---- for ( of__ok_for_iter_sugar__0 = 0; of__ok_for_iter_sugar__0 < len( buf ); of__ok_for_iter_sugar__0++ ) {
        mov     word [of__ok_for_iter_sugar__0], 0
.L5:
        mov     ax, [of__ok_for_iter_sugar__0]
        cmp     ax, 4
        jae     .L7                         ; unsigned <
; ---- buf[ of__ok_for_iter_sugar__0 ] = buf[ of__ok_for_iter_sugar__0 ] + 1
        mov     ax, [of__ok_for_iter_sugar__0]
        mov     bx, ax
        mov     al, [buf + bx]
        xor     ah, ah                      ; u8 -> u16
        inc     ax
        mov     bx, [of__ok_for_iter_sugar__0]
        mov     [buf + bx], al
; ---- total = total + buf[ of__ok_for_iter_sugar__0 ]
        mov     ax, [total]
        push    ax                          ; save lhs: rhs is not a leaf
        mov     ax, [of__ok_for_iter_sugar__0]
        mov     bx, ax
        mov     al, [buf + bx]
        xor     ah, ah                      ; u8 -> u16
        mov     bx, ax
        pop     ax
        add     ax, bx
        mov     [total], ax
.L6:
        inc     word [of__ok_for_iter_sugar__0]
        jmp     .L5
.L7:
; ---- for ( g = 0; g < len( mob ); g++ ) {
        mov     word [g], 0
.L9:
        mov     ax, [g]
        cmp     ax, 3
        jae     .L11                        ; unsigned <
; ---- mob[ g ].hp = 100
        mov     ax, [g]
        shl     ax, 1                       ; word elements
        mov     bx, ax
        mov     word [mob__hp + bx], 100
.L10:
        inc     word [g]
        jmp     .L9
.L11:
; ---- for ( of__ok_for_iter_sugar__0 = 0; of__ok_for_iter_sugar__0 < len( mob ); of__ok_for_iter_sugar__0++ ) {
        mov     word [of__ok_for_iter_sugar__0], 0
.L13:
        mov     ax, [of__ok_for_iter_sugar__0]
        cmp     ax, 3
        jae     .L15                        ; unsigned <
; ---- mob[ of__ok_for_iter_sugar__0 ].hp = mob[ of__ok_for_iter_sugar__0 ].hp + 1
        mov     ax, [of__ok_for_iter_sugar__0]
        shl     ax, 1                       ; word elements
        mov     bx, ax
        mov     ax, [mob__hp + bx]
        inc     ax
        mov     bx, [of__ok_for_iter_sugar__0]
        shl     bx, 1                       ; word elements
        mov     [mob__hp + bx], ax
; ---- mob[ of__ok_for_iter_sugar__0 ].x = 2
        mov     ax, [of__ok_for_iter_sugar__0]
        mov     bx, ax
        mov     byte [mob__x + bx], 2
.L14:
        inc     word [of__ok_for_iter_sugar__0]
        jmp     .L13
.L15:
; ---- for ( f = 0; f < len( cells ); f++ ) {
        mov     word [f], 0
.L17:
        mov     ax, [f]
        cmp     ax, 8
        jae     .L19                        ; unsigned <
; ---- cells[ f ] = 3
        mov     ax, [f]
        mov     bx, ax
        mov     dx, 0xB800                  ; segment of cells
        mov     es, dx
        mov     byte [es:bx], 3
.L18:
        inc     word [f]
        jmp     .L17
.L19:
; ---- for ( of__ok_for_iter_sugar__0 = 0; of__ok_for_iter_sugar__0 < len( cells ); of__ok_for_iter_sugar__0++ ) {
        mov     word [of__ok_for_iter_sugar__0], 0
.L21:
        mov     ax, [of__ok_for_iter_sugar__0]
        cmp     ax, 8
        jae     .L23                        ; unsigned <
; ---- cells[ of__ok_for_iter_sugar__0 ] = 4
        mov     ax, [of__ok_for_iter_sugar__0]
        mov     bx, ax
        mov     dx, 0xB800                  ; segment of cells
        mov     es, dx
        mov     byte [es:bx], 4
.L22:
        inc     word [of__ok_for_iter_sugar__0]
        jmp     .L21
.L23:
; ---- for ( of__ok_for_iter_sugar__1 = 0; of__ok_for_iter_sugar__1 < len( buf ); of__ok_for_iter_sugar__1++ ) {
        mov     word [of__ok_for_iter_sugar__1], 0
.L25:
        mov     ax, [of__ok_for_iter_sugar__1]
        cmp     ax, 4
        jb      .L28                        ; unsigned <
        jmp     .L27
.L28:
; ---- for ( of__ok_for_iter_sugar__0 = 0; of__ok_for_iter_sugar__0 < len( buf ); of__ok_for_iter_sugar__0++ ) {
        mov     word [of__ok_for_iter_sugar__0], 0
.L29:
        mov     ax, [of__ok_for_iter_sugar__0]
        cmp     ax, 4
        jae     .L31                        ; unsigned <
; ---- total = total + buf[ of__ok_for_iter_sugar__1 ] + buf[ of__ok_for_iter_sugar__0 ]
        mov     ax, [total]
        push    ax                          ; save lhs: rhs is not a leaf
        mov     ax, [of__ok_for_iter_sugar__1]
        mov     bx, ax
        mov     al, [buf + bx]
        xor     ah, ah                      ; u8 -> u16
        mov     bx, ax
        pop     ax
        add     ax, bx
        push    ax                          ; save lhs: rhs is not a leaf
        mov     ax, [of__ok_for_iter_sugar__0]
        mov     bx, ax
        mov     al, [buf + bx]
        xor     ah, ah                      ; u8 -> u16
        mov     bx, ax
        pop     ax
        add     ax, bx
        mov     [total], ax
.L30:
        inc     word [of__ok_for_iter_sugar__0]
        jmp     .L29
.L31:
.L26:
        inc     word [of__ok_for_iter_sugar__1]
        jmp     .L25
.L27:

; ---- implicit exit ----
        mov     word [_ax], 0x4C00          ; DOS terminate, exit code 0
        call    int21

; ==================================================== int helpers ====
; One per distinct interrupt: the literal is baked in, so the register
; sync is emitted once rather than at every call site.

int21:
        push    es
        mov     ax, [_ax]
        mov     bx, [_bx]
        mov     cx, [_cx]
        mov     dx, [_dx]
        mov     si, [_si]
        mov     di, [_di]
        int     0x21
        mov     [_ax], ax
        mov     [_bx], bx
        mov     [_cx], cx
        mov     [_dx], dx
        mov     [_si], si
        mov     [_di], di
        pop     es
        ret

; ============================================================ data ====

; ---- reserved globals: the machine registers ----
_ax:            dw      0
_al:            equ     _ax
_ah:            equ     _ax + 1
_bx:            dw      0
_bl:            equ     _bx
_bh:            equ     _bx + 1
_cx:            dw      0
_cl:            equ     _cx
_ch:            equ     _cx + 1
_dx:            dw      0
_dl:            equ     _dx
_dh:            equ     _dx + 1
_si:            dw      0
_di:            dw      0

; ---- variables ----
i:              dw      0        ; u16
of__ok_for_iter_sugar__0: dw      0        ; u16
g:              dw      0        ; u16
f:              dw      0        ; u16
of__ok_for_iter_sugar__1: dw      0        ; u16
total:          dw      0        ; u16

; ---- arrays ----
mob__x:         times 3 db 0        ; u8[3]
mob__hp:        times 3 dw 0        ; u16[3]
buf:            times 4 db 0        ; u8[4]

; ============================================================ heap ====
; No storage is emitted - a .COM owns everything past its image, so
; these are addresses and NASM does the arithmetic.

_hstack:        equ     258        ; 2 worst-case + 256 interrupt reserve
_htop:          equ     0FFFEh - _hstack

_hsize:         dw      _htop - _heap        ; NASM computes this
        align   2                           ; keep the u16 view aligned
_heap:
_heapw:         equ     _heap        ; same bytes, u16 view
